<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:InnoversUI.Controls">


    <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Width" Value="25"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <ControlTemplate.Resources>
                        <Geometry x:Key="CheckMarkData">M10,17L5,12L6.41,10.58L10,14.17L17.59,6.58L19,8M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3</Geometry>
                        <Geometry x:Key="UnCheckMarkData">M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M19,5V19H5V5H19Z</Geometry>
                        <Duration x:Key="AnimationDuration">0:0:0:0.3</Duration>
                        <Storyboard x:Key="ActiveAnimation">
                            <DoubleAnimation Storyboard.TargetName="ActiveIcon" Storyboard.TargetProperty="Opacity" From="0" To="1" FillBehavior="HoldEnd" Duration="{StaticResource AnimationDuration}" />
                            <DoubleAnimation Storyboard.TargetName="InactiveIcon" Storyboard.TargetProperty="Opacity" From="1" To="0" FillBehavior="HoldEnd" Duration="{StaticResource AnimationDuration}" />
                        </Storyboard>

                        <Storyboard x:Key="InactiveAnimation">
                            <DoubleAnimation Storyboard.TargetName="ActiveIcon" Storyboard.TargetProperty="Opacity" From="1" To="0" FillBehavior="HoldEnd" Duration="{StaticResource AnimationDuration}" />
                            <DoubleAnimation Storyboard.TargetName="InactiveIcon" Storyboard.TargetProperty="Opacity" From="0" To="1" FillBehavior="HoldEnd" Duration="{StaticResource AnimationDuration}" />
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid>
                        <controls:InnoIcon x:Name="ActiveIcon" IconData="{StaticResource CheckMarkData}" Fill="{TemplateBinding Foreground}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" MaxWidth="{TemplateBinding Width}" MaxHeight="{TemplateBinding Height}" HorizontalAlignment="Center" />
                        <controls:InnoIcon x:Name="InactiveIcon" IconData="{StaticResource UnCheckMarkData}" Fill="{TemplateBinding Foreground}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" MaxWidth="{TemplateBinding Width}" MaxHeight="{TemplateBinding Height}" HorizontalAlignment="Center" />
                    </Grid>

                    <!-- Animations -->
                    <ControlTemplate.Triggers>
                        <!-- ON -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource ActiveAnimation}">
                                    
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource InactiveAnimation}">
                                </BeginStoryboard>
                            </Trigger.ExitActions>

                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Silver"/>
                            <Setter Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CheckBoxSwitchStyle" TargetType="CheckBox">
        <Setter Property="Width" Value="35"/>
        <Setter Property="Height" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <!-- Track (fond) -->
                        <Border x:Name="SwitchTrack"
                    CornerRadius="10"
                    Background="LightGray"/>

                        <!-- Thumb (bouton rond) -->
                        <Border x:Name="SwitchThumb"
                    Width="13" Height="13"
                    CornerRadius="8"
                    Background="White"
                    Margin="2,2,0,2"
                    HorizontalAlignment="Left"
                    RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="ThumbTranslate"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" BlurRadius="4" ShadowDepth="1"/>
                            </Border.Effect>
                        </Border>
                    </Grid>

                    <!-- Animations -->
                    <ControlTemplate.Triggers>
                        <!-- ON -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Fond -->
                                        <ColorAnimation
                                Storyboard.TargetName="SwitchTrack"
                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                To="#4CAF50" Duration="0:0:0.2"/>
                                        <!-- Mouvement -->
                                        <DoubleAnimation
                                Storyboard.TargetName="ThumbTranslate"
                                Storyboard.TargetProperty="X"
                                To="16" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Retour fond -->
                                        <ColorAnimation
                                Storyboard.TargetName="SwitchTrack"
                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                To="LightGray" Duration="0:0:0.2"/>
                                        <!-- Retour position -->
                                        <DoubleAnimation
                                Storyboard.TargetName="ThumbTranslate"
                                Storyboard.TargetProperty="X"
                                To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                            
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="SwitchTrack" Property="Background" Value="#CCC"/>
                            <Setter TargetName="SwitchThumb" Property="Background" Value="#EEE"/>
                            <Setter TargetName="SwitchThumb" Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>