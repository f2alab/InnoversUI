<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <!--COLORS-->
    <SolidColorBrush x:Key="BorderColor">Gray</SolidColorBrush>
    <SolidColorBrush x:Key="BackColor">Gray</SolidColorBrush>
    <SolidColorBrush x:Key="ForegroundColor">White</SolidColorBrush>
    <Geometry x:Key="CalendarIconData">M9,10V12H7V10H9M13,10V12H11V10H13M17,10V12H15V10H17M19,3A2,2 0 0,1 21,5V19A2,2 0 0,1 19,21H5C3.89,21 3,20.1 3,19V5A2,2 0 0,1 5,3H6V1H8V3H16V1H18V3H19M19,19V8H5V19H19M9,14V16H7V14H9M13,14V16H11V14H13M17,14V16H15V14H17Z</Geometry>
    
    
    <!--DATEPICKER BUTTON STYLE-->
    <Style x:Key="DatePickerButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Border x:Name="Border" Background="Teal" CornerRadius="0 4 4 0" Margin="2 2">
                            <Grid >
                                <Viewbox Height="24" Width="24" VerticalAlignment="Center">
                                    <Path Data="{StaticResource CalendarIconData}" Fill="White" Height="24" Width="24"/>
                                </Viewbox>
                            </Grid> 
                        </Border>
                       
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF036565" TargetName="Border"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="Gray" TargetName="Border"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!--DATEPICKER TEXTBOX STYLE-->
    <Style x:Key="DatePickerTextBoxStyle" TargetType="{x:Type DatePickerTextBox}">
        <Setter Property="Height" Value="{DynamicResource DefaultFieldHeight}"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Outfit"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="10 0 0 0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                    <Border
                        x:Name="Border"
                        BorderThickness="0"
                        BorderBrush="Transparent">
                        <Grid>
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                Grid.Column="0"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden"/>
                        </Grid>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!--DATEPICKER STYLE-->
    <Style TargetType="{x:Type DatePicker}">
        <Setter Property="Margin" Value="0 0 0 0"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <Grid>
                        <Border
                            x:Name="Border"
                            Grid.ColumnSpan="2"
                            Height="{DynamicResource DefaultFieldHeight}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="2"
                            CornerRadius="{DynamicResource DefaultBorderRadius}"
                            Background="White">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox
                                    x:Name="PART_TextBox"
                                    Grid.Column="0"
                                    Style="{StaticResource DatePickerTextBoxStyle}"
                                    />
                                <Button
                                    x:Name="PART_Button"
                                    Grid.Column="1"
                                    Style="{StaticResource DatePickerButtonStyle}"
                                    Width="{DynamicResource DefaultFieldHeight}"
                                    Height="{DynamicResource DefaultFieldHeight}"/>
                                
                                <Popup
                                    x:Name="PART_Popup"
                                    AllowsTransparency="True"
                                    Placement="Bottom"
                                    PlacementTarget="{Binding ElementName=PART_TextBox}"
                                    StaysOpen="False"
                                    HorizontalOffset="15"
                                    PopupAnimation="Slide"/>
                                <Label
                                    x:Name="WaterMark"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"
                                    Foreground="Gray"
                                    Visibility="Collapsed"
                                    Padding="10 0 0 0"
                                    Margin="2 0 0 0"
                                    Content="{TemplateBinding Tag}"
                                    FontSize="12"
                                    FontWeight="DemiBold"
                                    Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="DeepSkyBlue" TargetName="Border" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="BorderBrush" Value="Gray" TargetName="Border" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True" SourceName="PART_TextBox">
                            <Setter Property="BorderBrush" Value="DeepSkyBlue" TargetName="Border" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value=""/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" TargetName="WaterMark"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>